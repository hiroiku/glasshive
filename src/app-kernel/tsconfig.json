// 葉である。src の他のどこも import しない — paths に 1 つも登録していないので、
// 書いた途端に「その名前は無い」でビルドが落ちる。
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "noEmit": false,
    "emitDeclarationOnly": true,
    "declarationDir": "../../dist/types/app-kernel",
    "tsBuildInfoFile": "../../dist/types/app-kernel.tsbuildinfo",
    "rootDir": ".",
    "paths": {
      "~/app-kernel/*": ["./*"]
    }
  },
  "include": ["."],
  "references": []
}
