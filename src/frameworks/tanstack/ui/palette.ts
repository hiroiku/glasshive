/* 数の並びに色を割り当てる。

   **黄金角で色相を回す。** 等分に回すと、本数が変わるたびに全部の色が入れ替わる。
   黄金角なら、後ろに足しても前の色は動かず、隣どうしも混ざらない。

   Git の線とモデルの内訳で同じ関数を使う。同じ画面に並ぶので、
   別々に決めると、無関係な 2 つが同じ色で並んで関係があるように見える。 */
export const laneColor = (index: number): string => `hsl(${(205 + index * 137.5) % 360} 58% 62%)`;
